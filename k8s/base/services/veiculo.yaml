apiVersion: v1
kind: Service
metadata:
  name: veiculo
  labels:
    app.kubernetes.io/name: veiculo
    app.kubernetes.io/part-of: oficina-mecanica
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: veiculo
  ports:
  - name: http
    port: 8082
    targetPort: http
    protocol: TCP

