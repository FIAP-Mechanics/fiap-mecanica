apiVersion: v1
kind: Service
metadata:
  name: atendimento
  labels:
    app.kubernetes.io/name: atendimento
    app.kubernetes.io/part-of: oficina-mecanica
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: atendimento
  ports:
  - name: http
    port: 8086
    targetPort: http
    protocol: TCP

