apiVersion: v1
kind: Service
metadata:
  name: funcionario
  labels:
    app.kubernetes.io/name: funcionario
    app.kubernetes.io/part-of: oficina-mecanica
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: funcionario
  ports:
  - name: http
    port: 8083
    targetPort: http
    protocol: TCP

