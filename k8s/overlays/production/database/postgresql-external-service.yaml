apiVersion: v1
kind: Service
metadata:
  name: postgres-mecanica
  labels:
    app.kubernetes.io/name: postgres-mecanica
    app.kubernetes.io/component: external-database
    app.kubernetes.io/part-of: oficina-mecanica
spec:
  type: ExternalName
  externalName: postgres.example.internal
  ports:
  - name: postgresql
    port: 5432
    targetPort: 5432
    protocol: TCP
